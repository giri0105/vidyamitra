/**
 * Firebase Stub - Firebase has been removed.
 * This file exists only to prevent import errors from any remaining references.
 * All data operations now go through /src/lib/api.ts → SQLite backend.
 */

// Stub exports so that any lingering imports don't crash
export const auth = null;
export const db = null;
export const storage = null;
export const functions = null;

console.log('ℹ️ Firebase removed — VidyaMitra uses SQLite via REST API');

export default null;
